# 3. [local] 독자 실행 모드 (나 혼자 뜰래!)
# 👉 'local' 프로필일 때만 이 설정들이 적용됩니다.

server:
  port: 8080 # ⚠️ (수정됨) server 설정은 eureka 밑이 아니라 최상위 레벨에 있어야 합니다.

spring:
  config:
    activate:
      on-profile: "local"

  h2:
    console:
      enabled: true
      path: /h2-console

  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driverClassName: org.h2.Driver
    username: sa
    password:

  jpa:
    show-sql: true
    # H2를 쓰지만 MySQL 문법을 호환해서 쓰겠다는 설정
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true

  cloud:
    config:
      enabled: false

eureka:
  client:
    enabled: false
    fetch-registry: false

springdoc:
  api-docs:
    version: openapi_3_0
  swagger-ui:
    path: /swagger-ui.html
# 3. [local] 독자 실행 모드 (나 혼자 뜰래!)
# 👉 'local' 프로필일 때만 이 설정들이 적용됩니다.

server:
  port: 8080 # ⚠️ (수정됨) server 설정은 eureka 밑이 아니라 최상위 레벨에 있어야 합니다.

spring:
  config:
    activate:
      on-profile: "local"

  h2:
    console:
      enabled: true
      path: /h2-console

  datasource:
    driver-class-name: org.h2.Driver
    # 👇 (중요) ;MODE=MySQL 추가!
    # (~/test는 홈디렉토리에 파일생성, 메모리 모드로 더 빠르게 하려면 jdbc:h2:mem:testdb;MODE=MySQL 사용)
    url: jdbc:h2:~/test;MODE=MySQL
    username: sa
    password:

  jpa:
    show-sql: true
    # H2를 쓰지만 MySQL 문법을 호환해서 쓰겠다는 설정
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create # 매번 실행 시 테이블 새로 생성

  cloud:
    config:
      enabled: false

eureka:
  client:
    enabled: false
    fetch-registry: false

springdoc:
  api-docs:
    version: openapi_3_0
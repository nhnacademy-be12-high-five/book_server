spring:
  application:
    name: book-server-test

  cloud:
    config:
      enabled: false
      discovery:
        enabled: false
    discovery:
      enabled: false

eureka:
  client:
    enabled: false
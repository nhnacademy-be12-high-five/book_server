spring:
  profiles:
    active: dev

  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-sever
      fail-fast: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/